#include <bits/stdc++.h>
using namespace std;
#define ll long long
const ll MAXN = 50010;
ll n, l;
ll c[MAXN];
ll dp[MAXN];
struct Node {
  ll l, r;
  
};
int main() {
  ios::sync_with_stdio(false);
  cin >> n >> l;
  for (int i = 1; i <= n; ++i) {
    cin >> c[i];
  }

  return 0;
}